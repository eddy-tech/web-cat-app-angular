import { SocialNumberPipe } from './social-number.pipe';

describe('SocialNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
